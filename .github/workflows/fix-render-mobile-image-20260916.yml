name: Publish approved render marketing visual
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Render is now committed directly to the repository.'
